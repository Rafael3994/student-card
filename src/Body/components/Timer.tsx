import { View, Text } from "react-native";

const Timer = () => {
    return (
        <View style={{ backgroundColor: 'grey', height: 100 }}>
            <Text>TIMER</Text>
        </View>
    )
};

export default Timer;